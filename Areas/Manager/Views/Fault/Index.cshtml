@model List<Fault>

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <h2 class="text-white py-2 text-center">Fault List</h2>

        <div class="row mt-3">
            <div class="col-md-7 d-flex flex-wrap gap-2">
                <select id="statusFilter" class="form-select w-auto">
                    <option value="">Status (all)</option>
                    <option value="Usterka naprawiona">Fixed</option>
                    <option value="Usterka nie naprawiona">Unfixed</option>
                </select>

                <select id="driveFilter" class="form-select w-auto">
                    <option value="">Drivable (all)</option>
                    <option value="Tak">Yes</option>
                    <option value="Nie">No</option>
                </select>

                <select id="carFilter" class="form-select w-auto">
                    <option value="">Asset (all)</option>
                    @foreach (var p in ViewBag.CarPlates as IEnumerable<string>)
                    {
                        <option value="@p">@p</option>
                    }
                </select>

                <select id="userFilter" class="form-select w-auto">
                    <option value="">User (all)</option>
                    @foreach (var u in ViewBag.Users as IEnumerable<string>)
                    {
                        <option value="@u">@u</option>
                    }
                </select>
            </div>

            <div class="col-md-5 text-end">
                <button id="exportBtn" class="btn btn-success me-2">
                    <i class="bi bi-file-earmark-excel"></i> Excel
                </button>
                <a asp-area="Manager" asp-controller="Fault" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Add Fault
                </a>
            </div>
        </div>
    </div>

    <div class="card-body p-4">
        <table id="faultTable" class="table table-bordered table-striped" width="100%">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Registration Number</th>
                    <th>Description</th>
                    <th>Reported by</th>
                    <th>Status</th>
                    <th>Drivable</th>
                    <th>Comment</th>
                    <th>Fix description</th>
                    <th>Date reported</th>
                    <th>Date fixed</th>
                    <th>Fixed by</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>

<!-- FIX MODAL -->
<div class="modal fade" id="fixModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">

            <form id="fixForm">
                <div class="modal-header">
                    <h5 class="modal-title">Mark as fixed</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <div class="modal-body">
                    <input type="hidden" id="faultId" />

                    <div class="mb-3">
                        <label class="form-label">Fix description</label>
                        <textarea id="fixDescription"
                                  class="form-control"
                                  rows="3"
                                  required></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Fix date</label>
                        <input id="fixDate"
                               type="date"
                               class="form-control"
                               required />
                    </div>

                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-secondary"
                            data-bs-dismiss="modal"
                            id="cancelFix">
                        Cancel
                    </button>

                    <button type="submit"
                            class="btn btn-success">
                        Save
                    </button>
                </div>

            </form>

        </div>
    </div>
</div>

@section Scripts {

    <script>
        $(document).ready(function () {

            var table = $('#faultTable').DataTable({
                "ajax": {
                    "url": "/Manager/Fault/GetFaults",
                    "type": "GET",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "id" },
                    { "data": "carNumberPlate" },
                    { "data": "description" },
                    { "data": "user" },
                    {
                        "data": null,
                        "render": function (data, type, row) {
                            const statusText = row.status;
                            const checked = row.isFixed ? "checked" : "";
                            const disabled = row.isFixed ? "disabled" : "";

                            if (type === "filter" || type === "sort") {
                                return statusText;
                            }

                            return `
                                <div class="form-check form-switch">
                                    <input class="form-check-input fixToggle"
                                           type="checkbox"
                                           data-id="${row.id}"
                                           ${checked}
                                           ${disabled}>
                                    <label class="form-check-label">
                                        ${statusText}
                                    </label>
                                </div>`;
                        }
                    },
                    {
                        "data": "isDrivable",
                        "render": d => d === "Tak"
                            ? "<span class='badge bg-info'>Yes</span>"
                            : "<span class='badge bg-secondary'>No</span>"
                    },
                    { "data": "comment", defaultContent: "-" },
                    { "data": "fixDescription", defaultContent: "-" },
                    {
                        "data": "dateReported",
                        "render": function (d) {
                            if (!d) return "-";
                            return new Date(d).toLocaleDateString();
                        }
                    },
                    {
                        "data": "fixDate",
                        "render": function (d) {
                            if (!d) return "-";
                            return new Date(d).toLocaleDateString();
                        }
                    },
                    {
                        "data": null,
                        "render": function (row) {
                            if (!row.isFixed) return "-";
                            return row.fixedByUser
                                ? row.fixedByUser.fullName ?? row.fixedByUser.email
                                : "Unknown user";
                        }
                    },

                    {
                        "data": "id",
                        "render": function (data) {
                            return `
                                <a href="/Manager/Fault/Upsert/${data}" class="btn btn-outline-primary mx-2">
                                    <i class="bi bi-pencil"></i> Edit
                                </a>
                                <button onclick="deleteFault(${data})" class="btn btn-outline-danger mx-2">
                                    <i class="bi bi-trash3"></i> Delete
                                </button>`;
                        }
                    }
                ],
                "responsive": true,
                "language": {
                    "search": "Search:",
                    "lengthMenu": "Show _MENU_ faults",
                    "info": "Showing _START_ to _END_ of _TOTAL_ faults",
                    "zeroRecords": "No matching faults found",
                    "infoEmpty": "No faults available",
                    "paginate": {
                        "first": "First",
                        "last": "Last",
                        "next": "Next",
                        "previous": "Previous"
                    }
                }
            });

        $('#statusFilter').on('change', function () {
            const v = $(this).val();
            const pattern = v ? '^' + v + '$' : '';
            table.column(4).search(pattern, true, false).draw();
        });

        $('#driveFilter').on('change', function () {
            table.column(5).search(this.value).draw()
        });

        $('#carFilter').on('change', function () {
            table.column(1).search(this.value).draw()
        });

        $('#userFilter').on('change', function () {
            table.column(3).search(this.value).draw()
        });

        $('#exportBtn').on('click', function () {
            const qs = $.param({
                status: $('#statusFilter').val(),
                drive: $('#driveFilter').val(),
                car: $('#carFilter').val(),
                user: $('#userFilter').val()
            });
            window.location = `/Manager/Fault/ExportToExcel?${qs}`;
        });

        let toggledCheckbox = null;

        $('#faultTable').on("change", ".fixToggle", function () {

            if ($(this).is(":checked")) {
                toggledCheckbox = $(this);
                $("#faultId").val($(this).data("id"));
                $("#fixModal").modal("show");
            }
            else {
                const id = $(this).data("id");

                $.ajax({
                    url: `/Manager/Fault/ToggleFixed/${id}`,
                    type: "POST",
                    success: function () {
                        $('#faultTable').DataTable().ajax.reload(null, false);
                    }
                });
            }
        });

        $("#cancelFix").on("click", function () {
            if (toggledCheckbox)
                $(toggledCheckbox).prop("checked", false);
        });

        $("#fixForm").on("submit", function (e) {
            e.preventDefault();

            $.ajax({
                url: "/Manager/Fault/MarkFixed",
                type: "POST",
                data: {
                    id: $("#faultId").val(),
                    description: $("#fixDescription").val(),
                    date: $("#fixDate").val()
                },
                success: function () {
                    $("#fixModal").modal("hide");
                    $('#faultTable').DataTable().ajax.reload(null, false);
                }
            });
        });

        });

        function deleteFault(id) {
            Swal.fire({
                title: 'Delete confirmation',
                text: 'Are you sure you want to delete this fault?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, delete',
                cancelButtonText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: `/Manager/Fault/DeleteFault/${id}`,
                        type: "DELETE",
                        success: function (response) {
                            if (response.success) {
                                Swal.fire('Success', response.message, 'success')
                                    .then(() => {
                                        $('#faultTable').DataTable().ajax.reload();
                                    });
                            } else {
                                Swal.fire('Error', response.message, 'error');
                            }
                        },
                        error: function () {
                            Swal.fire('Error', 'An error occurred while processing the request.', 'error');
                        }
                    });
                }
            });
        }
    </script>
}